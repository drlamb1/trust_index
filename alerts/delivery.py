"""
EdgeFinder — Alert Delivery (Phase 4)

Delivers alerts and daily briefings via multiple channels.

Phase 4 implementation will include:
  - Email (aiosmtplib + SendGrid/SMTP)
  - Slack (incoming webhook)
  - Discord (webhook)
  - ntfy.sh (push notifications, no account needed)
  - Local dashboard SSE (already wired via core/events.py)

All delivery is async and non-blocking.
Failed deliveries are logged but don't break the pipeline.
"""

# Phase 4 stub
raise ImportError("delivery.py is implemented in Phase 4. See README.md.")
